services:
  postgres:
    image: postgres:17
    restart: always
    environment:
      POSTGRES_USER: covound
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: covound
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
